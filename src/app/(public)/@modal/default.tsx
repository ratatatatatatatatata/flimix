/**
 * Default for the @modal parallel slot — nothing is shown unless an
 * intercepted /movie/[slug] or /series/[slug] navigation fills the slot.
 */
export default function ModalDefault() {
  return null;
}
